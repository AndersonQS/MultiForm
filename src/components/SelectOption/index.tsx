import React from 'react';

import * as C from './styles';
type Props = {
  title: string,
  description: string,
}
const SelectOption = ({title, description}: Props) => {
  return (
    <C.Container>...</C.Container>
  );
}

export default SelectOption;